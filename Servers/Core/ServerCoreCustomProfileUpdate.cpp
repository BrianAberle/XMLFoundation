// put your code own code in this file and replace this example...


else if (strSection.CompareNoCase("MyApp") == 0)
{
	if (strEntry.CompareNoCase("MyInt") == 0)
	{
		nMyGlobalInteger = atoi(pzNewValue);
	}
}